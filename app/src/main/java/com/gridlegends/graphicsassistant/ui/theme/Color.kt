package com.gridlegends.graphicsassistant.ui.theme

import androidx.compose.ui.graphics.Color

// ── 主色调 ──────────────────────────────────────
/** 青色主色 - 霓虹科技感 */
val NeonCyan = Color(0xFF00E5FF)
val NeonCyanDark = Color(0xFF00B8D4)
val NeonCyanLight = Color(0xFF6EFFFF)

// ── 强调色 ──────────────────────────────────────
/** 橙色强调 - 操作按钮/高亮 */
val AccentOrange = Color(0xFFFF6D00)
val AccentOrangeDark = Color(0xFFC43E00)
val AccentOrangeLight = Color(0xFFFF9E40)

// ── 警告色 ──────────────────────────────────────
/** 红色警告 */
val WarningRed = Color(0xFFFF1744)

// ── 背景色 ──────────────────────────────────────
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkSurfaceVariant = Color(0xFF2A2A2A)
val DarkCard = Color(0xFF252525)

// ── 文字色 ──────────────────────────────────────
val TextPrimary = Color(0xFFE0E0E0)
val TextSecondary = Color(0xFF9E9E9E)
val TextDisabled = Color(0xFF616161)

// ── 分区标题色 ──────────────────────────────────
val SectionDisplay = Color(0xFF00E5FF)    // 青色 - 显示设置
val SectionQuality = Color(0xFF76FF03)    // 绿色 - 画面质量
val SectionPostProcess = Color(0xFFFF6D00) // 橙色 - 后处理
val SectionReflection = Color(0xFFE040FB)  // 紫色 - 反射
val SectionTrackDetail = Color(0xFFFFAB00) // 黄色 - 赛道细节

// ── 修改标记色 ──────────────────────────────────
val ModifiedHighlight = Color(0x3300E5FF)  // 半透明青色背景
